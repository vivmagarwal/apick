{
  "name": "apick-workspace",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r --filter=\"./packages/*\" build",
    "test": "pnpm build && pnpm --filter @apick/blackbox test",
    "typecheck": "pnpm -r typecheck",
    "llms": "node scripts/build-llms.mjs",
    "llms:check": "node scripts/build-llms.mjs --check"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
